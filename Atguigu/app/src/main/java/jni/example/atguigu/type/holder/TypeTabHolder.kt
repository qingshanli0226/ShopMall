package jni.example.atguigu.type.holder

import android.view.View
import jni.example.atguigu.AllPowerfulHolder

class TypeTabHolder(itemView: View) : AllPowerfulHolder(itemView) {
    override fun <T> setDate(date: T) {

    }
}